my:
 name: forezp
 age: 12
 number:  ${random.int}
 uuid : ${random.uuid}
 max: ${random.int(10)}
 value: ${random.value}
 greeting: hi,i'm  ${my.name}

spring:
 datasource:
  driver-class-name: com.mysql.jdbc.Driver
  url: jdbc:mysql://121.199.51.229:3306/test
  username: root
  password: root88
 profiles:
  active: dev
 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true